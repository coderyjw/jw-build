const message = 'this is jw-build-test'

module.exports = function(api,options) {
  const { getConfigWebpack } = api
  
  const config = getConfigWebpack();
  config
  // 修改 entry 配置
  .entry('index')
    .add('src/index.js')
    .end()
  // 修改 output 配置
  .output
    .path('dist')
    .filename('[name].bundle.js');
    console.log(message, config.toConfig())
  }